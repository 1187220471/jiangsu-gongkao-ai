# 2026-06-24 Web端

## 账号绑定（绑号制）
- 去掉 `User.openid` 的 `@unique` 约束
- 新增 `BindToken` 模型（5 分钟有效、单次使用）
- 新增 `/api/auth/bind-token` 和 `/api/auth/bind-wechat` 接口
- 修改 `/api/auth/wechat-login`：`findUnique` → `findFirst`
- Web 个人中心新增「绑定小程序」入口
- 已推送到 GitHub 并部署到 Vercel

## 品牌清理
- 全面删除「戴锦鲤」/「daijinli」/「daijingli」相关内容
- 更新日志文件名和内容同步替换
- package.json / README / project.config 等元数据更新

## 申论老师名称匿名化
- 数据库 `ShenlunTeacherAnswer` 表 186 条记录更新
- 原始 JSON 数据源同步替换
- 12 个老师名称按「首字替换为某」规则匿名化

## 待办
- 题型自动分类（申论和真题复盘均存在"未分类"占位）
